**/.circleci
**/.editorconfig
**/.dockerignore
**/.git
**/.DS_Store
**/.vscode
**/node_modules

# Specific patterns to ignore
data-node
meili_data*
librechat*
# Keep the committed deploy config in the build context so `COPY . .` bakes it
# into the image (Railway has no volume mount). Only ${ENV} placeholders inside.
!librechat.yaml
Dockerfile*
docs

# Ignore all hidden files
.*
